# 动态数码管显示

## 目的

在显示数码管的基础上让数码管动态显示

## 主要代码

`Delay()`函数（详见笔记“独立按键控制LED状态”）

`Nixie(Location,Number)`函数（详见笔记“点亮静态数码管”）

### 例子

		while(1){
			Nixie(1,1);
		  Delay(200);
		  Nixie(2,2);
		  Delay(200);
		  Nixie(3,3);
			Delay(200);
		}

上述代码可实现“第一个数码管显示数字1”“第二个数码管显示数字二”“第三个数码管显示数字3”这三个事件依次交替循环进行。

### 发现问题

如果调快频率，会发现指定数字没有显示在正确的数码管上

#### 原因分析

单片机数码管显示是先选择位置再决定点亮该位置的指定段，因为单片机频率太快了，导致选段环节容易被跳过

#### 修正方式

对`Nixie(Location,Number)`函数进行修正，每次选择段后添加清零环节

	Delay(1);
	P0=0x00;

因为闪烁较快，因此我们看到的是同时显示了123