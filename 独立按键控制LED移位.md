# 独立按键控制LED移位

## 目的

通过按键来实现LED按顺序移位

当L1亮时，按键可以实现L2亮L1灭，再次按则实现L3亮L2灭，以此类推。

## 核心代码

通过位运算实现上述功能

`<<`和`>>`为位运算

00000001<<1 = 00000010，意为向左位移一位
11000000>>2 = 00110000，意为向右位移两位

先初始化`P2=~0x01`，让P2初始亮L1

并且`unsigned char LEDNum=0;`，让LEDNum初始为0

然后进行按键判定

LEDNum为需要在0x01的基础上位移位数，每次按键`LEDNum++;`

每次按下都要进行越界判定

			if(LEDNum>=8){
				LEDNum=0;
			}

P2亮灯为`(0x01<<LEDNum)`的按位取反

### 最后的代码

		if(P3_1==0){
			Delay(20);
			while(P3_1==0);
			Delay(20);
			
			LEDNum++;
			if(LEDNum>=8){
				LEDNum=0;
			}
			P2=~(0x01<<LEDNum);
		}

## 功能拓展

上述代码可以实现按下K1则让LED向右移位，可添加代码实现按下K2让LED向左移位。

在上述代码中，把`P3_1`替换为`P3_0`可实现用K2进行控制

越界判定改成

```
			if(LEDNum==0){
				LEDNum=7;
			}
```

并且每按一次执行`LEDNum--;`

等效于0x01>>向右位移