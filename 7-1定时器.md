# 定时器

## 介绍

51单片机的定时器属于单片机的内部资源，其电路的连接和运转均在单片机内部完成

## 作用

（1）用于计时系统，可实现软件计时，或者使程序每隔一固定时间完成一项操作

（2）替代长时间的Delay，提高CPU的运行效率和处理速度

## STC89C52的定时器

单片机有个定时器，T0和T1与传统的51单片机兼容，T2是此型号单片机增加的资源

## 定时器工作原理

定时器根据时钟输出的信号，每隔一个单位时间，计数单元数值加一

当计数单元数值增加到“设定的闹钟提醒时间”时，计数单元会向中断系统发出中断申请，产生“响铃提醒”，使程序跳转到中断服务函数中执行
$$
时钟 \to计数单元\to中断系统
$$

## 定时器工作模式

T0和T1均有四种工作模式

模式0：13位定时器/计数器
模式1：16位定时器/计数器
模式2：8位自动重装模式
模式3：两个8位计数器

## 中断系统

中断系统是为使CPU具有对外界紧急事件的实时处理能力而设置的

当CPU处理事情时外界发生紧急事件诉求，要求CPU暂停当前工作，转而处理紧急事件，处理完再回到中断的地方。这样的过程称为中断，实现这种功能的部件被称为中断系统，请示CPU中断的请求源称为中断源

一般允许多个中断源，每个中断源拥有一定的优先级

### 中断源

STC89C52有8个中断源

外部中断0、定时器0中断、外部中断1、定时器1中断、串口中断、
外部中断2、外部中断3……

中断优先级数4

## 寄存器

寄存器是连接软硬件的媒介

在单片机中寄存器就是一段特殊的RAM存储器

寄存器可以存储和读取数据

每个寄存器背后都连接了一根导线，控制着电路的连接方式，寄存器相当于一个复杂机器的“操作按钮”

TCON为定时器/计数器T0、T1控制寄存器

TF1：定时器/计数器T1溢出标志
TR1：定时器T1的运行控制位
TF0：定时器/计数器T0溢出中断标志
TR0：定时器T0的运行控制位
IE1：外部中断1请求源标志
IT1：外部中断1触发方式控制位

PT0：定时器0中断优先控制位