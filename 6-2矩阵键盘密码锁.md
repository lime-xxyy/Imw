# 矩阵键盘密码锁

## 目的

通过代码实现矩阵键盘密码锁，大部分代码可参考笔记“6-1矩阵键盘”

## 核心代码

在初始化之后添加代码`LCD_ShowString(1,1,"Password:");`提示输入矩阵键盘密码。

`unsigned int Password;`在main函数外定义宏整型变量Password

因为十进制四位数密码（int很难将五位数及以上储存完整），所以只采用前十个按键

			if(KeyNum<=10){
				Password=KeyNum%10;
			}

将密码变成按键对应的数，取余的目的是将10转化为0
用于获取一位密码

			if(KeyNum<=10){
				Password=Password*10;
				Password=Password+KeyNum%10;
			}

上述代码则可以实现获取四位密码
原理：先获取第一位密码，*10用于向左移一位再获取第二位密码，直至四位密码都获取

当输入第五位密码时，密码栏会输出错误的数字，因此应该限制输入的位数

```
if(KeyNum<=10){
				if(Count<4){
				Password=Password*10;
				Password=Password+KeyNum%10;
				}
				Count++;
				LCD_ShowNum(2,1,Password,4);
			}	
```

先在main函数前面定义宏整型变量Count（因为是宏，自动为0）,然后每输入一位Count（位数）+1，并且判断位数是否等于4，等于4则停止输入

### 其他功能的实现

#### 密码确定功能

矩阵键盘比十进制数字按键多出6个键，可以分别扮演不同的角色

```
		if(KeyNum==11){
			if(Password==2345){
				LCD_ShowString(1,14,"OK ");
				Password=0;
				Count=0;
				LCD_ShowNum(2,1,Password,4);
			}
		}
```

上述代码可以赋予按键S11密码输入确认功能
通过`if(Password==2345)`比较输入的密码与预期密码来判断是否正确

当正确时，显示OK

				else{
					LCD_ShowString(1,14,"EER");
					Password=0;
					Count=0;
					LCD_ShowNum(2,1,Password,4);
				}

当输入密码错误时，显示ERR

当位数大于等于4时无法再输入密码，所以每次输入密码时，不管正确与否，都要对密码以及位数清零`Password=0;Count=0;`

`LCD_ShowNum(2,1,Password,4);`用于刷新显示密码，使其显示0000

注意：显示的OK后面加一个空格，否则当前面输错时，如果后面输入正确，“OK”的字符串长度无法完全覆盖“ERR”

#### 密码取消功能

			if(KeyNum==12){
				Password=0;
				Count=0;
				LCD_ShowNum(2,1,Password,4);
				}

按键S12成为取消密码功能

上述代码可以取消输入的密码，通过对密码与位数的清零实现