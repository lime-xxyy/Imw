# LED流水灯

## 流水灯普通版

LED利用二进制来操作亮灭，代码需要转化为十六进制。

通过P2=FE->FD->FB->F7->EF->DF->BF->7F来实现，每条代码之间加入`Delay500ms()`函数来增加切换间隔。

## 流水灯高阶版

制作一个可以更改间隔的LED流水灯

通过设立一个变量函数来灵活地设立间隔时间

先复制出一个切换间隔为1ms的函数

在函数中设立变量（`Delay1ms(unsigned int xms)`）

在该函数中设立while循环

```
while(xms){

xms--;
}
```

将原函数中下列部分复制到`xms--;`的前面

```
i = 2;
j = 199;
 do
 {
    while (--j);
 } while (--i);
```

最后再将函数套用在LED流水灯普通版，即可实现可变间隔流水灯。

在函数括号内填入数值，100为100毫秒。