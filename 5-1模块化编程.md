# 模块化编程

## 模块化编程与传统方式编程的区别

### 传统方式编程

所有**函数均放在main.c**里，一个文件会因为模块多而代码增多

不利于代码的组织与管理，影响编程者思路

### 模块化编程

把各个模块的代码放在不同的.c文件里，在**.h文件**里**提供外部可调用函数**的声明

其他.c文件向使用其中的代码时，只需要阅读“#include XXX.h”

可提高代码的可阅读性、可维护性、可移植性

## 文件类型

### .c文件

函数、变量的定义

### .h文件

可被外部调用的函数、变量的声明

#### 注意事项

任何自定义的变量、函数在调用前必须有定义或声明

使用到的自定义函数的.c文件必须添加到工程参与编译

使用到的.h文件必须要放在编译器可寻找到的地方

## C语言预编译

以#开头，作用是在真正的编译开始之前，对代码做一些处理

`#include <REGX52.H>`：把REGX52.H文件的内容搬到此处

`#define PI 3.14`：定义PI，把PI替换成3.14

`#define ABC`：定义ABC

`#ifndef __XX_H__`：如果未定义`__XX_H__`

`#ifdef __XX_H__`：如果定义`__XX_H__`

`#endif`：结束

### 例子

#### （1）

```
#ifndef AAA
#define AAA
int a;
#endif
```

第一行`#ifndef AAA`：如果未定义AAA，则（预）执行该行之后，`#endif`之前的内容（如果AAA已定义，指定内容将不再执行，即不再次定义AAA，并且**跳过**定义整形变量a

第二行`#define AAA`：满足`#ifndef AAA`时（预）定义AAA

第三行`int a;`：满足`#ifndef AAA`时（预）定义整形变量a

第四行`#endif`：结束`#ifndef AAA`条件

#### （2）

```
#ifdef AAA
sadffbg
#endif
```

上述代码第二行，显而易见会报错

第一行`#ifdef AAA`：如果定义了AAA，执行到`#endif`

所以当未定义AAA时，sadffbg不会被预处理（即不会被编译），因此系统不会报错

若之前定义了AAA，则会报错

## 具体实例

以之前的`void Delay(unsigned int xms)`（详见笔记“LED流水灯”）函数为例，可以把它作为一个模块，分别创建Delay.c和Delay.h文件（Delay.c要确保在项目内，Delay.h可以不在项目内，不过也最好放进去）

**Delay.c**：放入`void Delay(unsigned int xms)`函数

**Delay.h**：填入下面代码，详细参考上文

```
#ifndef __DELAY_H__
#define __DELAY_H__

void Delay(unsigned int xms);

#endif
```

