# 独立按键控制LED灯显示二进制

## 目的

实现：每按一次按钮，LED灯可以实现二进制+1。

如：11100111->11101000
00010001->00010010

## 代码实现

### 方法1

`P2=FF`（灯全灭）

每按一次`P2--;`

		if(P3_1==0){
			Delay(20);
			while(P3_1==0);
			Delay(20);

采用上述代码来实现按键判定（具体流程可见笔记“独立按键控制LED灯状态”）

### 方法2

初始化`unsigned char LEDNum=0;`

此时`LEDNum`是从0开始

			LEDNum++;
			P2=~LEDNum;

每按一次实行上述代码，LEDNum+1，LED灯进行对应数值的按位取反。