# 独立按键控制LED灯状态

## 目标

当LED灯是灭的，实现按一次点亮LED，，再按一次熄灭LED。

## 代码实现

		if(P3_1==0){
			Delay(20);
			while(P3_1==0){
				
			}
			Delay(20);
			P2_0=~P2_0;
		}

修改`Delay()`函数（具体方法参考笔记“LED流水灯”）

因为刚按下按钮时（20ms之内）不稳定，所以延后20ms进行判定按钮是否松开

`P2_0=~P2_0;`让LED灯通过按位取反实现亮灭交替。
