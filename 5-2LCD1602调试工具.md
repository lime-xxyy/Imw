# LCD1602调试工具

使用LCD1602液晶屏作为调试窗口，提供类似**printf函数**的功能，可实时观察单片机**内部数据的变化情况**，便于**调试**和**演示**。

LCD1602代码属于模块化代码，只需知道所提供函数的作用和使用方法即可使用LCD1602

## 函数解析

| 函数                             | 作用                 |
| -------------------------------- | -------------------- |
| `LCD_Init();`                    | 初始化               |
| `LCD_ShowChar();`                | 显示一个字符         |
| `LCD_ShowString(1,3,"Hello");`   | 显示字符串           |
| `LCD_ShowNum(1,9,123,3);`        | 显示十进制数字       |
| `LCD_ShowSignedNum(1,13,-66,2);` | 显示有符号十进制数字 |
| `LCD_ShowHexNum(2,1,0xA8,2);`    | 显示十六进制数字     |
| `LCD_ShowBinNum(2,4,0xAA,8);`    | 显示二进制数字       |

*（这个头文件由“江协科技”提供）*

## 使用方法

LCD1602调试工具外观是绿色电路板上面贴一个液晶显示屏，背面是一排排插头

51单片机右上角有一排排插口，使用时将调试工具的插头插入插口里

使用时原单片机数码管与LED不再发挥作用

### 相关代码使用

在`void main()`函数内部，`while(1)`循环前面，使用`LCD_Init()`进行初始化

```
void main(){
	LCD_Init();
	while(1){
		
	}
}
```

`LCD_ShowChar(Column,Line,'A');`会在指定位置显示指定字符（`LCD_ShowChar(1,2,'A');`可以在1行1列显示字符A）

`LCD_ShowString(1,3,"Hello");`即可在1行，从第三列开始依次显示Hello

`LCD_ShowNum(1,9,123,3);`即可在1行，从第九列开始依次显示123这3位数

`LCD_ShowSignedNum(1,13,-66,2);`对比`LCD_ShowNum(1,9,123,3);`可以显示有符号的数，即可以显示负数，长度不包括符号

`LCD_ShowHexNum(2,1,0xA8,2);`可以在指定位置显示十六进制数，长度即为十六进制数位数本身

`LCD_ShowBinNum(2,4,0xAA,8);`可以在指定位置显示二进制数，代码中输入的数字要是十六进制数