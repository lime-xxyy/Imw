# 独立按键

## 轻触按键

### 类型

相当于一种电子开关

### 方式

按下时开关接通，松开时开关断开

### 原理

通过轻触按键内部的金属弹片受理，实现接通和断开

#### 按键的抖动

机械触点弹性作用使开关闭合及断开时瞬间会伴随一连串的抖动
即开关在闭合时不会马上稳定地接通，断开时也不会一下子断开。

### 代码实现

单片机第一个按钮是K1，代码用`P3_1`表示，0表示按下，1表示松开

```
if(P3_1==0){

}
```

上述代码可以实现按下按钮可执行什么操作

## 用按键操控LED灯

```
if(P3_1==0){
P2_0=0;
}else{
P2_0=1;
}
```

`if(P3_1==0)`：当按钮按住时，`P2_0=0`：LED灯亮起

否则熄灭

外面嵌套一层`while(1)`循环可以永久执行。

### 逻辑组合

```
if(P3_1==0 && P3_0==0){
P2_0=0;
}else{
P2_0=1;
}
```

此处P3_0为按键K2

此代码能实现当同时按下K1与K2才能使P2_0灯亮起，松开任意按键都会熄灭。

同理`if(P3_1==0 || P3_0==0)`可以实现按下K1或者K2都可以点亮P2_0

