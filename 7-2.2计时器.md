# 用LCD1602实现计时功能

## 头文件使用

```
`#include <REGX52.H>`
`#include "Delay.h"`
`#include "LCD1602.h"`
`#include "Timer0.h"`
```

需要延时函数（详见5-1模块化编程），LCD1602函数（详见5-2“LCD1602调试工具”），以及定时器函数（详见7-2"按键控制LED流水灯模式"）

在main函数下面需要添加中断函数（详见7-2"按键控制LED流水灯模式"）

## main函数解析

```
void main(){
	LCD_Init();
	Timer0Init();
	LCD_ShowString(1,1,"Clock:");
	LCD_ShowString(2,1,"  :  :");
	while(1){
		LCD_ShowNum(2,1,Hour,2);
		LCD_ShowNum(2,4,Min,2);
		LCD_ShowNum(2,7,Sec,2);
	}
}
```

main函数负责LCD1602的显示功能，用于塑造计时器界面

`LCD_ShowString(1,1,"Clock:");`在第一行显示Clock:
`LCD_ShowString(2,1,"  :  :");`用于秒分时之间的冒号

`LCD_ShowNum(2,1,Hour,2);`显示时位
`LCD_ShowNum(2,4,Min,2);`显示分位
`LCD_ShowNum(2,7,Sec,2);`显示秒位

## 中断函数解析

```
void Timer0_Routine() interrupt 1
{
	static unsigned int T0Count;
	TL0 = 0x18;		//设置定时初值
	TH0 = 0xFC;		//设置定时初值
	T0Count++;
	if(T0Count>=1000){
		T0Count=0;
		Sec++;
		if(Sec>=60){
			Sec=0;
			Min++;
			if(Min>=60){
				Min=0;
				Hour++;
				if(Hour>=24){
					Hour=0;
				}
			}
		}
	}
}
```

中断函数负责时分秒的计算与进位

每过一秒，Sec数值加1，并且判断Sec是否达到60，Min是否达到60，以及Hour是否达到24

Sec达到60，会触发Min数值加1，Sec自身数值归零

Min达到60，会触发Hour数值加1，Min自身数值归零