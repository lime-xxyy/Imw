# 串口向电脑发送信息

## 头文件

Delay.h（详见笔记5-1“模块化编程”）

## 串口相关寄存器配置

### 相关概念

#### SCON：串行控制寄存器（可位寻址）

SM0/FE (7)：当PCON寄存器中的SMOD0/PCON.6位为1时，该位用于帧错误检测。当检测到一个无效停止位时，通过UART接收器设置该位，它必须由软件清零

当PCON寄存器中的SMOD0/PCON.6位为0时，该位和SM1一起指定串行串行通信的工作方式

SM2 (5)：允许方式2或方式3多机通信控制位

REN (4)：允许/禁止串行接收控制位（REN=0禁止接收，REN=1允许接收）

TI：发射中断请求标志位，当发射完了TI=1，必须用软件复位（0）

RI：接收中断请求标志位

#### PCON：电源控制寄存器

SMOD：波特率选择位

SMOD0：帧错误检测有效控制位

### 便捷操作

打开STC-ISP，在右栏找到**波特率计算器**，系统频率选择**12MHz**，波特率选择**4800**，UART选择**串口1**，UART数据位选择**8位**，波特率发生器选择**定时器1（8位自动重载）**,定时器时钟选择**12T**，勾选**波特率倍速**

## 代码实现

[^采用双八位]: 就是十六位记的数多，但每次都需要自己写的代码赋初值，浪费时间。双八位就是将十六位分开，一个计数，另一个存放初值，每次计数完成后AR会自动将值赋给CNT，不用代码处理，比较快，但只有八位所以记的数少了。

配置好后，每次使用前先初始化

```
void UART_Init(){
	SCON=0x40;
	PCON|=0x80;
	TMOD &= 0x0F;		//设置定时器模式
	TMOD |= 0x20;		//设置定时器模式
	TL1 = 0xF4;		//设置定时初值
	TH1 = 0xF4;		//设置定时初值
	TR1 = 1;		//定时器0开始计时
	ET1=0;


}
```

### 单片机的发送

编写一个`void UART_SendByte()`函数来实现单片机发送功能

```
void UART_SendByte(unsigned char Byte){
	SBUF=Byte;
	while(TI==0){
		
	}
	TI=0;

}
```

SBUF是发射内容，让SBUF=Byte可以指定发射的内容

```
void main(){
	UART_Init();
	UART_SendByte(0x66);
	while(1){
		

	}

}
```

初始化之后进行发送66

### 发现问题

当把发送函数放至while(1)循环里面，会发射错误的数字，这是晶振误差所导致的，因此需要引用Delay()函数

	while(1){
		UART_SendByte(0x66);
		Delay(1);
	}

加入delay即可减少误差

# 电脑通过串口控制LED

因为是单片机接收电脑的数据，所以REN位要变成1，即SCON=01010000=0x50

其他位和串口向电脑发送信息一样，则不用变

还需要添加`EA=1;`（总中断允许位）和`ES=1;`（串行口中断允许位）

因为是串口中断，所以是interrupt 4，创建子函数`void UART_Routine() interrupt 4`，加上interrupt 4成为串口中断子函数

```
void UART_Routine() interrupt 4{
	if(RI==1){
		P2=~SBUF;
		UART_SendByte(SBUF);
		RI=0;
	}
}
```

在中断子函数内部，当有接收中断请求时，则可以用`UART_SendByte(SBUF)`函数来输入想要单片机执行的指令（如`P2=~SBUF;`是操控LED灯亮灭）然后用软件把RI调零

## 数据显示模式

HEX模式/十六进制模式/二进制模式：以原始数据的形式显示

文本模式/字符模式：以原始数据编码后的形式显示